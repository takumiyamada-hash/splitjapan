# クエリファンアウト（①④）

生成AIは、聞かれた1問をそのまま検索しない。裏で質問を分解し、周辺の疑問まで自動的に広げて
（= クエリファンアウト）から答えの材料を探す。だから**本命の1問だけに答えを持っていても足りない**。
買い手がたどる質問の道筋を先に地図にして、そのどこを聞かれても自分の名前が出る状態を作る＝「点ではなく面」。

## 正本と回し方

ビルド工程が無いサイトなので、全部 `node scripts/*.mjs` で回す。

| コマンド | 何をするか |
|---|---|
| `npm run fanout` | 質問地図と公開HTMLを突き合わせて FAQ/PAGE/NEAR/GAP を判定。`fanout-report.md` を出力 |
| `npm run answers` | 全ページの FAQPage を集めて `answers.html` を生成（手で書かない） |
| `npm run analytics` | 新しいHTMLに `analytics.js` の読み込みを挿入（冪等） |
| `npm run check` | 上3つをまとめて |

- 質問の地図: **`scripts/questions.json`**（本命10問 × 派生44問 = 54問）

### 判定の意味

| 判定 | 意味 | 打ち手 |
|---|---|---|
| `FAQ` | サイトのどこかに Question として構造化データで出ている（AIが1問として拾える） | 何もしなくてよい |
| `PAGE` | 本文には書いてあるが、質問の形になっていない | そのページに `<details>` と FAQPage を1問足す |
| `NEAR` | 近いFAQはあるが言い回しがずれている | 既存FAQの質問文を、実際に打ち込まれる言い回しに寄せる |
| `GAP` | どこにも無い | 次に書くもの。ただし事実が取れないものは書かない |

判定は補助。半分以上のページに出てくる語（`japan` `money` `cash` `card` など46語）は
ヘッダー/フッター由来の定型文とみなして採点から外している。

## 現状（2026-09-15）

| | 件数 | 割合 |
|---|---|---|
| FAQ | 23 | 43% |
| PAGE | 24 | 44% |
| NEAR | 7 | 13% |
| **GAP** | **0** | **0%** |

この回で `FAQ 16 → 23`、`GAP 5 → 0`。一番効いたのは、**トップページと
ピラーページに FAQ が1問も無かった**のを埋めたこと。

- **`index.html`（ツール本体）に FAQ を7問**追加。ここが一番集客すべきページなのに
  構造化データの Question が0だった。無料か／登録は要るか／データはどこに保存されるか／
  オフラインで動くか／精算レートは何を使うか／誰が誰に払うかの決め方／タックスフリー対応。
  ツールへの疑問はAIが答える前に必ず通る関門なので、ここが空だと紹介されない。
- **`guides/index.html`（ピラー）に FAQ を4問**追加。どのセクションにも属さない横断質問:
  出発前に両替すべきか／銀行に旅行を伝える必要があるか／入れなくていいアプリ／精算レート。

追加した11問はすべて既存の記述・README・llms.txt の検証済みファクトから書いた。新しい事実は作っていない。

## 面の受け皿

- **`answers.html` → `/answers`** — サイト中の FAQPage を全部集めて1ページに（現在 **51問**）。
  ガイドに FAQ を足したら `npm run answers` で再生成。手で書き写さない。
  スキーマは `ItemList` + `Question`（回答本文は載せない。本文はHTMLにあるので複製は転送量の無駄）。
- **`data.html` → `/data`** — 独自の数値（③）。行ごとに出典と確認日を持つ。
  「実効還付率 8.45% / 6.45%（10%ではない）」がこのサイトにしか無い数字。
- **`llms.txt`** — 上の2つを「まずここを見ろ」として案内している。

## 質問を足すとき

`scripts/questions.json` の `seeds[]` に追記する。

```json
{
  "id": "短いID",
  "q": "本命の質問（実際にAIに打ち込まれる言い回しで）",
  "stage": "discovery | core | comparison | logistics",
  "followups": [
    { "q": "その答えを聞いた人が次に必ず聞くこと", "must": ["判定用キーワード"] }
  ]
}
```

`must` には定型文フィルタを通り抜ける固有語を入れる（`betsu-betsu` `felica` `3-d secure` `8.45` など）。
`japan` `money` `cash` のような語は効かない。

コツは「自分がこの答えを聞いたら、次に何を聞き返すか」を1問ずつ足すこと。
本命を増やすより、派生を深くした方が面になる。

## 次に埋めるべき穴（GAPは0だがNEARが7ある）

NEAR は答えを持っているのに言い回しがずれている状態。7問のうち効くのは:

- 「How do you split a bill in Japan?」— 既存は "Why won't Japanese restaurants split the bill?"。
  **why ではなく how で聞かれる**ので、how の形の質問も1つ用意する。
- 「How much cash do you need in Japan?」— 既存は "How much cash should I bring to Japan?"。
  `need` は検索でも生成AIでも `bring` より多い言い回し。
- 「Should I get a Suica card for my Japan trip?」— 判断を求める形の質問に答えがない。
  Suica ガイドは罠の話から始まっていて、「そもそも作るべきか」に答えていない。
