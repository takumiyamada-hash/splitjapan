# 土台：評判と第三者からの言及（記事を書く前に埋めるもの）

事例（Go Fish Digital, 3ヶ月でAI経由の成約+83%・成約率25倍）で一番大事なのは施策の中身ではなく、
その会社が記事の中で正直に書いた一文だった:

> この結果が出たのは、うちにはすでに評判と権威性があったからだ。
> Googleビジネスプロフィール・Yelp・Quora で評判が定着していたので、評判作りをする必要がなかった。

生成AIは、レビューの内容や、第三者がその会社をどう語っているかも判断材料にしている。
**記事の施策の効き方は、土台の厚みにそのまま比例する。** 無名だと無理という話ではなく、順番の問題。

## このサイトの立ち位置（正直な棚卸し）

このサイトは他の3サイトと決定的に違う点がある。**プロダクトがある。**
ガイドサイトはレビューの対象になりにくいが、**ツールはレビューされる。**
つまり土台を作る経路が最初から1本多い。そこを使っていないのが今の状態。

- 強み:
  - **`/data` の「実効還付率 8.45%」は他が間違えている数字。** 「Japan tax refund 10%」と
    書いているサイトが大量にある中で、二段階税率と手数料を踏んだ計算を出しているのはほぼ無い。
    *他人の間違いを正す数字*は、引用されるときに最も強い形。
  - 出典と確認日をページ単位で持っている。`llms.txt` に「レンジと日付で引用してくれ」と明記してある。
    この正直さはAIから見た信頼性そのもの。
  - GitHub にリポジトリが公開されていて、`sameAs` で結ばれている（＝実在の確認先がある）。
- 弱み:
  - **レビューが1件も無い。** 無料ツールなのに、使った人の声がどこにも無い。
  - 第三者がこのサイトについて語っている場所がほぼ無い。
  - **計測がまだゼロ**（`AI_MEASUREMENT.md` の有効化手順を踏むまで、何が効いたか分からない）。

## 優先順（上から順に、1件ずつ）

### 0. 先に計測を入れる（これだけは施策より前）

`analytics.js` の `GA_ID` を埋める。5分で終わる。
これをやらないうちに下をやると、何が効いたか永遠に分からない。事例が最初に
やったのもこれ（②）だった。

### 1. ツールがレビューされる場所に出す（このサイト固有の近道）

無料・登録不要・オフライン動作のツールは、それだけで紹介されやすい。

- **Product Hunt / Hacker News（Show HN）** — 「Japan旅行の割り勘、登録なしでリンク1本」は
  そのまま刺さる切り口。HNは特に「サーバーに何も上げない」設計を評価する層がいる。
  投稿すると実際のフィードバックが第三者言及として残る。
- **Reddit の r/JapanTravel / r/JapanTravelTips** — 自己宣伝ルールが厳しいので、
  **質問に答えるのを先にやる。** 「割り勘どうする？」系のスレッドは定期的に立つ。
  答えを直接書いて、必要なら最後に道具を出す。リンクだけの投稿はしない。
  これ経由の流入は `forum_referral` として GA4 に出る。
- **旅行系ブログ・YouTuberに数字を渡す** — 「還付率10%は間違い」は記事のネタになる。
  `/data` を「自由に引用してください」と渡す。売り込みではなくデータ提供。
- **GitHub** — リポジトリに README のスクリーンショットと「なぜ作ったか」を置く。
  Star は第三者評価として弱いが、実在性の裏付けにはなる。

▶ 実践法: **今月は1件だけ**。Show HN か r/JapanTravel の既存スレッドへの回答、どちらか。

### 2. 使った人の声を1件もらう

- ツール内に「うまくいった？」の一行と、返信先（`/about` のメール）を置く。
  旅行から帰った直後が一番書いてもらえるタイミング。
- もらった声は `/about` に掲載許可つきで載せる。**1件あるだけで**、
  「レビューが存在しないツール」から「使われているツール」に変わる。
- 星の数を作らない。数を盛るより、具体的な1件の方が効く。

▶ 実践法: 精算完了画面に一行足す。「Did this work? Tell me what broke.」

### 3. エンティティとしての実在性（コード側でできること）

- [x] `Organization.sameAs` に GitHub リポジトリ（既存）
- [x] `Person`（Takumi, Tokyo）が各記事の author に入っている（既存）
- [x] `/data` に `Dataset` と `citation`（5出典）を持たせた
- [ ] **`/about` に「掲載・引用されました」欄を作る。** 1. で言及が取れたらここに積む
- [ ] `sameAs` を増やす — 実際に運用している公開プロフィールだけ。放置アカウントは書かない

### 4. やらないこと

- レビューの自作、Product Hunt での相互投票依頼
- Reddit での宣伝目的の連投、複数アカウントでの自演
- 実体のない「〇〇に掲載」表記
- 存在しない `sameAs` の羅列

AIが見ているのは**第三者が自発的にどう語っているか**なので、作られた評判は土台にならない。

## 進捗（ここを更新していく）

| 日付 | やったこと | 結果 |
|---|---|---|
| 2026-09-15 | 計測の器を実装（GA_ID未設定）、`/answers`・`/data` 公開、トップとピラーにFAQ11問、質問地図と監査 | — |
